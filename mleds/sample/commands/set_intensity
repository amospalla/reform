// The set_intensity action sets the server brightness. It affects everything played,
// either movies or clients (actually, internal clients create movies and play them on
// the go).

action=set_intensity
// format: intensity=[+-]<float>
// examples: absoute number like 0.95 or relative like +0.1 or -0.1
value=0.1
end=true
